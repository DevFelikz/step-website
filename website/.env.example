DATABASE_URL="file:./dev.db"

# Minst 32 tecken i produktion
SESSION_SECRET="byt-till-en-lång-slumpmässig-sträng-minst-32-tecken"

# bcrypt — escapa varje $ som \ (Next @next/env expanderar $VAR annars)
# node -e "console.log(require('bcryptjs').hashSync('DITT_LOSENORD',10))"
# Exempel om hashen är $2b$10$abc... skriv: ADMIN_PASSWORD_HASH=\$2b\$10\$abc...
ADMIN_PASSWORD_HASH=
